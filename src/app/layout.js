export const metadata = {
  title: 'Dropshipping Tool',
  description: 'Automacao de dropshipping com Mercado Livre e AliExpress.',
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
